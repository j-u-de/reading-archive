import {NextResponse} from 'next/server';
export async function POST(req:Request){try{const b=await req.json();if(!b||typeof b.sessionId!=='string'||(!Number.isFinite(b.page)&&!b.chapter))return NextResponse.json({error:'sessionId and page or chapter are required'},{status:400});return NextResponse.json({sessionId:b.sessionId,page:Number.isFinite(b.page)?Math.max(0,b.page):null,chapter:typeof b.chapter==='string'?b.chapter.slice(0,200):null,logDate:new Date().toISOString().slice(0,10)},{status:201})}catch{return NextResponse.json({error:'Invalid progress data'},{status:400})}}
