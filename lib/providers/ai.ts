import {pythonProxy} from './python-proxy';
export type ReadingNote={one_sentence_summary:string;core_points:string[];reading_notes:string;insights:string[]};
export interface TextAIProvider{generateReadingNote(input:{title:string;author?:string;reflection?:string}):Promise<ReadingNote>;generateTopics(input:{title:string;description?:string}):Promise<string[]>}
export class FallbackTextAIProvider implements TextAIProvider{async generateReadingNote(i:{title:string;author?:string;reflection?:string}){return {one_sentence_summary:`《${i.title}》的核心内容整理`,core_points:['核心观点待补充','可结合个人阅读感受继续完善','建议回顾原书重点章节'],reading_notes:i.reflection||'AI 辅助整理的阅读笔记。',insights:['将书中方法与实际生活结合']}}async generateTopics(i:{title:string;description?:string}){return ['待分类']}}
class DeepSeekProvider implements TextAIProvider { private fallback=new FallbackTextAIProvider(); private async ask(prompt:string){const d=await pythonProxy({kind:'ai',body:{model:process.env.AI_MODEL||'deepseek-v4-flash',temperature:0.2,messages:[{role:'system',content:'你是阅读档案助手，只返回合法 JSON，不要 Markdown。'},{role:'user',content:prompt}]}});return JSON.parse(d.choices?.[0]?.message?.content||'{}')}
 async generateReadingNote(i:{title:string;author?:string;reflection?:string}){try{return await this.ask(`为《${i.title}》${i.author||''}生成阅读笔记 JSON，字段 one_sentence_summary(string), core_points(string[]), reading_notes(string), insights(string[])。`)}catch(error){console.error('DeepSeek request failed',error);return this.fallback.generateReadingNote(i)}}
 async generateTopics(i:{title:string;description?:string}){try{const d=await this.ask(`为书籍《${i.title}》${i.description||''}提取 3-5 个主题，返回 JSON {topics:string[]}`);return Array.isArray(d.topics)?d.topics:[]}catch{return this.fallback.generateTopics(i)}}}
export const textAIProvider:TextAIProvider=new DeepSeekProvider();
